package program_22.java;

public class RunThread {
	public static void main(String args[])
	{
		CThread one = new CThread("First Thread");
		CThread two = new CThread("Second Thread");
		CThread three = new CThread("Third Thread");
	}
}